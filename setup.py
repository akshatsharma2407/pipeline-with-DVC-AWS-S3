from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='implementing piplelines, data versioning and data cloud storage (AWSs3) together.',
    author='Akshat',
    license='',
)
